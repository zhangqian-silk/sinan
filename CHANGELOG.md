# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- **题号系列成为一个筛选维度**：`--series main / LCP / LCR / LCS / 面试题 / P / B`，
  网页端在「题目来源」旁多一个下拉，计数同样跟其余条件取交集
  （选了洛谷，系列一栏就只剩 P 和 B）。

- **在线版（GitHub Pages）**：`npm run site` 打出一个纯静态站，
  把随包基线连同编译好的 `store` / `planner` / `api` 一起发出去，在浏览器里
  装成同一个 Store —— 查询、分面、专题、学习计划全是同一份代码算的，
  网页与 `sinan plan` 的结果必然一致，不会出现两套实现走偏。
  为此把 `store.ts` 里的文件系统操作抽成了可注入的 `StoreHost`
  （Node 侧在 `host-node.ts`，浏览器侧在 `host-memory.ts`），
  并把 `serve.ts` 的接口逻辑拆到零 node 依赖的 `api.ts`。
  静态站的依赖链上只要混进任何 `node:` 导入，构建与测试会一起失败。
  在线版只有基线数据，所以没有题面、没有面试频次与官方题单；
  打卡改存浏览器 localStorage。
- **人工判定层**：逐题分析，按解法逐条写「这个技巧的核心思想落到本题是什么样子」，
  每条解法配一组标签 —— 常见解法都列，不是只留最简的那个。写在 `reviews/*.jsonl`（一批一个文件），
  有判定的题以人工为准，没有的仍走脚本。已评 3648 题 / 5160 种解法，
  有题面的力扣题已 100% 评完，全部主线与全部专题计划自然全覆盖，
  `sinan show` 和网页详情都把解法放在最前面。
  判定同时纠正了脚本的误标：除法求值曾被挂「区间 DP」、拼车被挂「单调栈」、
  用栈实现队列被挂「回溯」、合并 K 个升序链表被挂「二叉树遍历」等，
  各专题题池因此显著变干净（区间 DP 从 40 题收敛到 29 题）。
  同一道题在主站和「剑指 Offer」等副本里重复出现时，两侧判定必须完全一致，
  这条由测试强制：168 对副本里曾有 50 对标签对不上，现已全部对齐。
  洛谷题库里没有题面，人工判定目前只覆盖力扣侧，洛谷仍靠标签别名映射。
- `dist/tools/review-dump.js`：把一批题的题面压成纯文本连同现有标签打出来，供逐题评阅。
  只在本地读，不往仓库里写任何题面。
- `dist/tools/review-thin.js`：判定「偏薄」体检，找出可能漏了解法的判定。
  两个互相独立的信号：同族题（slug 只差序号）之间解法数落差过大，
  以及「中等/困难 + 高频 + 只写了一种解法」。单解法本身不是错，
  工具只报可疑、不报错，由人复核后决定要不要补。
  它来自一次真实失误 —— 清理脚本噪音标签时，把噪音碰巧指对的真解法一并删掉了：
  #769 只剩「前缀最大值等于下标」一种，而单调栈和「与排序后比前缀和」两条同样成立。
- **标签升级成一棵层级不齐的树**：13 个分区 / 65 个子标签 / 52 个技巧节点。
  节点底下有具名技巧就往下分一层，没有就地收尾 —— 最短路分三格、回溯分一格、
  哈希与计数不分。结构是递归的，将来长第四级不用改类型。
- 技巧节点的 id 直接复用思路 id，标签树与题解思路合成同一套 id、同一棵树：
  2226 道题挂上了深层标签（3565 条），挂上就意味着题解里真的用了这个技巧。
  深层标签要求父节点在场，所以树永远自洽。
- 路径寻址：`sinan learn math/number-theory/math-sieve`，写错祖先不认。
  `topics` / `learn` / `plan` / `list --tag` 都支持树上任意一层。
- 深层节点的讲解继承父节点的模板与常见坑，自己贡献识别信号和代表题。
- 洛谷补了 34 条标签别名（素数判断、进制、Tarjan、网络流…）。此前这些整片没被
  认出来：32 道带「素数/质数/筛法」的题只有 5 道拿到「数论」。现在数学分区
  870 → 965 题，洛谷的深层标签从 41 涨到 459。
- 原始标签直连深层节点的映射表（力扣 `monotonic-stack`、洛谷「素数判断」）——
  洛谷抓不到题解，这是它们唯一的深层证据来源。
- **装完什么都不做就能用**：包里自带 2.4 MB 的精简题库（基线），分类体系、
  每一类的解题思路、题目清单和平台链接开箱即有。`sinan sync` 抓来的完整数据
  落在 `~/.sinan`，优先级更高。
- `sinan learn` 不带参数就是**讲解总目录**：13 个大类 + 65 个子标签逐条列出
  一句话核心思路和题量，一条命令看完整个体系。
- 讲解页新增**代表题**：取自和 `plan` 同一套最小覆盖，按易→中→难各挑几道，
  每道给明文平台链接。大类会让每个子标签轮流露面。
- `sinan topics <大类>` 钻取时，每个子标签多给三道代表题和讲解入口。
- `sinan plan --links` 在每道题下多打一行明文链接（默认只做终端超链接）。
- 网页端新增「讲解」视图：总目录列出 13 个大类 + 65 个子标签的一句话核心思路，
  点进去是完整讲解加代表题，每道题带平台链接。内容与 `sinan learn` 同源。
- 新增 `/api/notes`（讲解总目录）；`/api/note` 带上代表题与题池规模；
  `/api/routes` 标出哪些主线要先 `sinan sync`。
- 新增 Web 接口测试：真起服务发 HTTP 请求，逐条校验视图依赖的字段，
  并断言网页与终端的代表题是同一批。
- 合规边界写死在代码里：随包只发题目的事实标识和我们自己的分析产出；题面原文、
  题解、通过率、CodeTop 频次、官方题单一律不发。`npm test` 有一条守着它 ——
  基线里出现超长文本或 HTML 片段就失败。
- 全部功能用 TypeScript 实现：CLI 15 个子命令、Web 端、抓取与打标构建。运行时零依赖。
- 打标走三条互相独立的证据通道（题解代码 / 自己读题面 / 题解措辞）并交叉印证，
  每道题都能翻出「我为什么这么判」。
- 学习计划：按真实解法思路算相似度，贪心最小覆盖挑代表题，4 条跨专题主线。
- 78 张教学卡片，识别信号直接从打标规则反查，与实际打标依据同源。
- 数据、题解目录、打卡记录三者的位置都可覆盖，`sinan where` 可查。
- 测试分五层：纯函数用例、教学卡片体检、planner/store 回归（入库迷你夹具）、
  CLI 冒烟、打标自检（82 条，需真实数据）。

### 修复

- 移动端顶部太占地方：390×844 上导航条加搜索栏吃掉 221px，占了 26% 的屏幕。
  真正的原因不是「字大」，是横排后的 flex 项没有 `flex-shrink: 0` ——
  它们被压到内容宽度以下，中文逐字换行，「刷题训练台」竖成三行，一条 44px 的导航条撑到 122px；
  搜索栏与统计数字也被挤成两行，页面标题同样竖排。现在该不折行的都不折行、
  放不下就横向滚（选中项会自动滚进视野），窄屏收起统计数字与「/ 聚焦」提示。
  顶部降到 106px、占 13%，首屏能多看到一整张卡片。

- 网页端换视图后滚动位置错乱：滚动条挂在 `.view` 容器上，换视图只换内容不动它，
  于是从标签体系深处点进讲解会落在讲解页的中段，退回来又变成讲解页留下的那个位置。
  另外清空容器会让浏览器把滚动条夹到 0，「再加载 60 题」和打卡因此都会把人甩回列表顶部。
  现在定位由 `render()` 统一负责：默认留在原处，换视图和换筛选条件时显式指定落点，
  往前走一律从顶部开始、往回走（「← 标签体系」与浏览器前进后退）还原原位置。
  顺带去掉 `.view` 上的 `scroll-behavior: smooth` —— 页面里没有锚点跳转，
  它只会让换页的重定位和新布局打架，落点漂几十像素。

- 「标签体系」和「讲解总目录」是同一棵 13 × 65 的树渲染了两遍，只是一边配进度条与
  定义式说明、一边配题量与核心思路，同一份目录要看两个页面。现在合成一页：
  标签树只在「标签体系」渲染一次，每个节点直接写讲解的一句话核心思路，
  进度、去练、讲解、排计划四个入口都挂在同一张卡上；定义式的 desc 挪到讲解详情页当副标题。
  `sinan learn`（不带参数）与 `#learn` 都落到这一页，`/api/notes` 随之下线。

- 按题号排序时 LCP / LCR / LCS / 面试题跟主站题交替出现（洛谷 B 也插在 P 中间）：
  排序键把题号里的非数字全抹掉了，`LCR 014` 和 `#14` 因此塌成同一个键。
  现在先按系列分段再比数值，面试题的 `08.09` 也改成逐段比而不是当成 `809`。

- 题面里的约束整段丢失：`1 <= n <= 200` 被 HTML 清洗当成标签吃掉了（正则 `<[^>]+>`
  会把 `<= n <` 当作一个标签）。现在只吃真正的标签，`show` 和网页的题面都恢复了约束。

- 网页端在随包基线下会显示「高频命中 0/0」「题单 0 份」，像是坏了。现在改成
  「一题多解」计数、题单页说明要先同步、需要频次的主线标成「需要同步」，
  题目详情不再显示「通过率 0%」。
- 「下一步」在随包基线下是空的 —— 它按面试频次找最薄弱专题，而基线里没有频次。
  刚装完、一道没刷恰恰最需要这个，改成退回入门主线：按先修顺序各取一道没刷的代表题。
- 目录页的代表题此前只按经典度（题号）排，「二叉树遍历」会拿 #22 括号生成 当门面 ——
  它确实被打了这个标签，但拿它介绍遍历只会误导。改成贴合度优先，权重按 20 分一档
  取整后再比经典度，门面变回 #94 中序遍历。
- 基线模式下没有面试频次，此前所有题权重都算成 1.0，经典度门槛全线失效，
  贪心一路奔向困难档（27 道里 22 道困难）。改用「题号越靠前越经典」当代理信号，
  难度结构恢复成 3 / 11 / 13。
- 相似度图在分数并列时按 slug 定序。此前顺序随哈希种子变，导致同一份原始数据
  重建两次，学习计划会不一样。
- 进度条在 `done > total` 时崩溃（`String.repeat` 不接受负数）。
- `topics` 的列宽此前按未替换的 `{PROG}` 占位符计算，多留了一格。
- `learn` 的题池提示因属性名拼错，从来没显示过。
